class TransactionRow:
    def __init__(self, fecha="", detalle="", referencia="", debitos="", creditos="", saldo=""):
        self.fecha = fecha
        self.detalle = detalle
        self.referencia = referencia
        self.debitos = debitos
        self.creditos = creditos
        self.saldo = saldo
